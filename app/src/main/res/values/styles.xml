<resources>

    <style name="AppTheme" parent="AppTheme.Base" />

    <style name="MyKeyboard">
        <item name="itemTextAppearance">?attr/itemTextAppearance</item>
    </style>

    <style name="MyKeyboardView" parent="MyKeyboard">
        <item name="keyTextSize">@dimen/keyboard_text_size</item>
    </style>

    <style name="MyKeyboardViewPopup" parent="MyKeyboard">
        <item name="keyTextSize">@dimen/preview_text_size</item>
    </style>

    <style name="MyKeyboard.Alert" parent="Theme.Material3.Light.Dialog.Alert">
        <item name="colorAccent">@color/color_primary</item>
        <item name="android:buttonBarNegativeButtonStyle">@style/DialogButtonStyle</item>
        <item name="android:buttonBarPositiveButtonStyle">@style/DialogButtonStyle</item>
        <item name="android:buttonBarNeutralButtonStyle">@style/DialogButtonStyle</item>
    </style>

    <style name="CircleImage" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">50%</item>
    </style>

    <!-- Single, seamless splash. Window background matches the SplashActivity's
         gradient so there is no blank flash before the activity draws (pre-12).
         On Android 12+, values-v31/styles.xml additionally tunes the system splash. -->
    <style name="SplashTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@drawable/splash_background</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
    </style>

    <style name="CalculatorButtonStyle">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">0dp</item>
        <item name="android:layout_columnWeight">1</item>
        <item name="android:layout_rowWeight">1</item>
        <item name="android:layout_margin">1dp</item>
        <item name="android:gravity">center</item>
        <item name="android:background">@drawable/keyboard_key_selector</item>
        <item name="android:textSize">@dimen/label_text_size</item>
        <item name="android:clickable">true</item>
        <item name="android:focusable">true</item>
    </style>

    <style name="DialogButtonStyle" parent="Widget.MaterialComponents.Button.TextButton">
        <item name="android:textColor">@color/color_primary</item>
    </style>
</resources>
